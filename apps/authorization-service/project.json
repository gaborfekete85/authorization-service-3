{
  "name": "authorization-service",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run gradle:build"
      }
    },
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run gradle:run"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run gradle:clean"
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run gradle:publish"
      }
    }
  }
}